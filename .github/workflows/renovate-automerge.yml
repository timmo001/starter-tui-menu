---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@fe592e0a559dc882914a1f21c8af7fee5362cb11 # master
    permissions:
      pull-requests: write
      contents: write
